<%= form_for(curricula) do |f| %>
  <% if curricula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curricula.errors.count, "error") %> prohibited this curricula from being saved:</h2>

      <ul>
      <% curricula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :created_at %>
    <%= f.datetime_select :created_at %>
  </div>

  <div class="field">
    <%= f.label :updated_at %>
    <%= f.datetime_select :updated_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
